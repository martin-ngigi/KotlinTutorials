<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
   <item
       android:id="@+id/mi_settings"
       android:title="Settings"
       android:icon="@drawable/ic_baseline_settings_24"
       app:showAsAction="ifRoom"/>
    <!-- if showAsAction="never" is used wont show the icon, but instead will show just a text-->
    <!-- if showAsAction="ifRoom"  if there is enough room, show icon+string-->

    <item
        android:id="@+id/mi_addContact"
        android:title="Contact"
        android:icon="@drawable/ic_baseline_person_add_24"
        app:showAsAction="always"/>

    <item
        android:id="@+id/mi_favourite"
        android:title="Favourite"
        android:icon="@drawable/ic_baseline_favorite_24"
        app:showAsAction="always"/>

    <item
        android:id="@+id/mi_feedback"
        android:title="Feedback"
        app:showAsAction="never"/>

    <item
        android:id="@+id/mi_close"
        android:title="Close App"
        app:showAsAction="never"/>
</menu>